# 汇总关于项目的所有提示词

- 指示灯优化
  你是一个专业的嵌入式固件开发架构师，现在请分析`drivers\output\indicators\indicator.c`下关于指示灯的实现，对该实现进行分析并进行架构优化。要求如下：

  1. 重点：先从架构层面进行优化，明确思路，不允许直接修改代码。输出.md文件到memory-bank目录下。
  2. 要求该指示灯驱动进行解耦，不与耦合其他部分。
  3. 基础流程，初始化设置几个灯、灯的GPIO口等基本参数，运行时设置指示灯指示状态，闪烁频率等参数，通过状态机切换状态。
  4. 指示灯采用状态机定时器。

  针对设计文档，有如下问题，1.分层级太多，导致实现简单功能文件复杂。需要简化。最好只有一个硬件对接层， 驱动层，应用层。三个.c 文件。2.不需要对之前的代码进行兼容，是完全的重构。3.暂时不需要考虑应用层的使用（例如定义回连灯效），只需要规范指示灯参数（在实际使用时只需要对不同状态设置不同参数即可满足要求），这样解耦兼容性更好。4.不需要考虑rgb和matrix，只通过gpio控制。请重新设计并更改文档。

- 自定义定时器
  你是一个专业的嵌入式固件开发架构师，现在请分析`c\timer_task\timer_task.c`下关于自适应定时器的实现，对该实现进行分析并进行架构优化。要求如下：

  1. 请先完全理解思路，并确保你获取了所有需求，如果有不明确的可以直接提问用户。
  2. 不要直接编写代码，先确认思路，然后输出.md 到当前目录下，阐述你的实现思路，最好包括了流程图等说明。
  3. 这是运用在定时器资源受限的嵌入式平台中，一个定时器需要兼顾运行多个定时任务，要考虑兼容性和准确度。
  4. 基本思想如下：1.每个时刻定时器间隔不同，通过一个定时器，取当前时刻最小定时时间为间隔。例如活动状态下需要5ms一个间隔扫描，同时如果存在led灯（250ms）闪烁或者其他定时任务。这时定时器为5ms定时。2s后进入idle，变为2s定时器（同时存在led闪烁）。这时定时器自动切换到250ms。这样兼顾了准确性和功耗。2.前提下是定时间隔都为整数倍。不允许定时4ms以下的定时器。3.任意定时器变更时自动计算当前的定时时间。4.需要考虑其他异常情况，例如：各任务定时时间公因数太小，需要编译前检查、计时器超长度等问题。

  针对设计文档有如下问题：1.请先完全理解思路，并确保你获取了所有需求，如果有不明确的可以直接提问用户。2.在切换时无论选择哪种策略，都会有影响定时任务的可能性，例如从2s切换到5ms时，会等2s执行时再切换，导致5ms定时器丢失一段时间。请评估一下技术实现方式，通过main循环计数器校准定时时间（假设main循环和主频相关，那每次计数都是固定间隔），使定时器时间立刻变更也影响不大。例如每次定时执行的时候，将main计数器清0。当需要切换定时时间时，通过mian计数器粗算经过时间再校准剩下执行时间。3.确认后变更.md文档后在更改代码。
  
  请根据ARCHITECTURE_DESIGN.md针对架构优化描述进行代码落地。更改timer_task.c代码。
  主循环计数中的main_loop_cnt会有溢出风险，请更改设计逻辑，每次定时触发时清0，另外一个计数器通过定时器间隔统计当前ms数。这样防止loop_time_us过小时引发main_loop_cnt溢出。请先阐述思路确认后再更改代码
  
  通过读取特定芯片api（c\timer_task\hal\timer.c）。发现硬件定时器存在timer_get_tick方法可以获取定时器计数。这样获取就不需要通过main_loop_cnt校准，请使用timer_get_tick优化，请先阐述思路确认后再更改代码。

请分析`middleware\keyboard`路径下有关按键实现流程，并总结一份完整文档放置在`middleware\keyboard`下。（更新或者删除这个目录下的README.md）
