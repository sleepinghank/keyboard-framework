## 1. Implementation

- [ ] 1.1 梳理并固化 CH584 蓝牙启动链路，明确 `system_hal_init` 与 `bt_driver_init` 职责边界。
- [ ] 1.2 调整 `system_init_coordinator` 的应用层初始化，使 `commu_service_init` 在启动阶段被调用。
- [ ] 1.3 调整 `wireless` 初始化流程，确保通过现有 driver 接口完成蓝牙驱动初始化与初始广播启动。
- [ ] 1.4 收敛断联后的重广播逻辑，避免重复启动广播和状态抖动。
- [ ] 1.5 在关键流程点补齐可观测日志，覆盖广播 连接 回连 断联 重广播路径。

## 2. Verification

- [ ] 2.1 使用 CH584 构建入口编译通过。
- [ ] 2.2 烧录并验证 广播 可被扫描。
- [ ] 2.3 烧录并验证 连接 成功和日志一致。
- [ ] 2.4 烧录并验证 断联 后自动重广播。
- [ ] 2.5 烧录并验证 回连 成功。
- [ ] 2.6 记录串口日志关键证据并回填结果。
