# KB904 蓝牙妙控键盘固件设计文档

> 文档版本: V1.2
> 创建日期: 2026-03-17
> 状态: 评审修订中
> 变更说明: V1.2 优化文件结构，减少新增文件，复用现有架构

---

## 目录

1. [需求摘要](#1-需求摘要)
2. [现状分析](#2-现状分析)
3. [架构设计](#3-架构设计)
4. [组件设计](#4-组件设计)
5. [数据流与错误处理](#5-数据流与错误处理)
6. [实施计划](#6-实施计划)

---

## 1. 需求摘要

### 1.1 产品概述

| 项目 | 规格 |
|------|------|
| **产品型号** | KB904 蓝牙妙控键盘 |
| **配对名称** | inateck NeoMagic Pro |
| **连接方式** | 蓝牙 BLE 5.3（单通道） |
| **目标系统** | iPad（默认） |
| **主控芯片** | WCH CH584M |
| **触控芯片** | Pixart PCT1336QN |
| **电池** | 500mAh / 3.7V 锂电池 |

### 1.2 交付范围

| 优先级 | 数量 | 状态 |
|--------|------|------|
| P0（核心功能） | 42 项 | ✅ 必须实现 |
| P1（重要功能） | 17 项 | ✅ 必须实现 |
| P2（增值功能） | 2 项 | ❌ 本次不实现 |

**明确排除的功能**：
- OTA 固件升级
- ZWSN 手势自定义系统（使用 iPad 原生 PTP）
- APP 配置接口
- 霍尔开关固件处理（纯硬件控制）
- 德语 (DE) 布局

### 1.3 边界条件

| 项目 | 值 |
|------|-----|
| 配对超时 | 60 秒 |
| 回连超时 | 60 秒 |
| 低电量预警阈值 | 20% |
| 背光关闭阈值 | 20% |
| 背光休眠时间 | 5 秒无操作 |
| 深度休眠时间 | 10 分钟无操作 |
| 长按识别时间 | 3 秒 |

### 1.4 丝印语种

| 语种 | 状态 |
|------|------|
| 美式英语 (US) | ✅ 支持 |
| 英式英语 (UK) | ✅ 支持 |

---

## 2. 现状分析

### 2.1 已完备模块

| 模块 | 路径 | 关键能力 |
|------|------|----------|
| 矩阵扫描 | `drivers/input/keyboard/matrix.c` | 8×16 矩阵、COL2ROW、GPIO 唤醒 |
| 防抖算法 | `drivers/input/keyboard/debounce.c` | sym_defer_pk 等多算法 |
| Combo 引擎 | `middleware/keyboard/combo/kb_combo_engine.c` | 状态机、长按检测、链表管理 |
| Fn 功能键 | `middleware/keyboard/combo/kb_fn_action.c` | 媒体键、系统键 |
| 背光驱动 | `drivers/output/backlight/backlight.c` | RGB PWM、亮度/颜色（7色） |
| 指示灯 | `drivers/output/indicators/indicator.c` | OSAL 事件驱动、多灯效 |
| 输出服务 | `application/service/output_service.c` | 指示灯协调、低电量事件占位 |
| 无线状态机 | `middleware/communication/wireless.c` | WT_* 状态转换 |
| LPM 管理 | `middleware/communication/lpm.c` | Idle/Deep 两级睡眠 |
| 触控板服务 | `component/touch_component/touchpad_service.c` | PTP 模式 |
| 存储 | `drivers/storage/storage.c` | EEPROM + CRC16 |

### 2.2 需修改/新增模块

| 模块 | 变更类型 | 说明 |
|------|----------|------|
| 背光颜色 | 修改 | 7色 → 13色 |
| 背光休眠 | **新增** | 5秒无操作熄灭（整合到 output_service） |
| Fn 组合键 | 修改 | 新增 Fn+右Cmd/右Shift/右Enter |
| 电量检查 | **新增** | Fn+右Cmd 闪烁指示（放在 kb_fn_action.c） |
| 低电量联动 | **新增** | ≤20% 关背光 + 红灯闪（复用现有机制） |
| 指示灯配置 | 修改 | 2灯 → 4灯（条件编译） |
| 产品配置 | **新增** | KB904 GPIO/矩阵配置 |

### 2.3 硬件资源评估

| 资源 | 总容量 | 当前使用 | 需新增 | 余量 |
|------|--------|----------|--------|------|
| Flash | 448KB | ~80KB | ~1.5KB | ~366KB (82%) |
| RAM | 32KB | ~16KB | ~128B | ~16KB (50%) |

**结论**：资源充足，无瓶颈。

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Application Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │ main.c      │  │ system_init │  │ output_service          │ │
│  │ (主循环)    │  │ (初始化)    │  │ (背光休眠+指示灯协调)   │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│                    Middleware Layer                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ keyboard    │  │ wireless    │  │ lpm         │             │
│  │ (矩阵+Combo)│  │ (蓝牙状态机)│  │ (低功耗)    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│  ┌─────────────┐  ┌─────────────┐                               │
│  │ fn_action   │  │ battery     │                               │
│  │ (Fn功能)    │  │ (电量检测)  │                               │
│  └─────────────┘  └─────────────┘                               │
├─────────────────────────────────────────────────────────────────┤
│                    Driver Layer                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ matrix      │  │ backlight   │  │ indicator   │             │
│  │ (矩阵扫描)  │  │ (RGB PWM)   │  │ (LED灯效)   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
├─────────────────────────────────────────────────────────────────┤
│                    HAL Layer (ch584)                             │
│  GPIO · PWM · ADC · I2C · UART · Power                          │
└─────────────────────────────────────────────────────────────────┘
```

**架构优化说明**：
- **背光休眠功能**整合到现有 `output_service.c`，而非新建 `backlight_service`
- **电量检查 Action** 放在现有 `kb_fn_action.c`，而非新建文件
- **指示灯配置**通过条件编译复用现有 `indicator_config.h`

### 3.2 文件变更清单（优化后）

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| **新增** | `keyboards/kb904/config.h` | KB904 产品配置（含 GPIO 宏） |
| **新增** | `keyboards/kb904/config_hw.h` | 矩阵 + GPIO 硬件映射 |
| **新增** | `keyboards/kb904/keymaps/layout_kb904.h` | 键位表 |
| **修改** | `keyboards/indicator_config.h` | 条件编译支持 4 灯 |
| **修改** | `drivers/output/backlight/backlight.h` | 13色枚举 |
| **修改** | `drivers/output/backlight/backlight.c` | 13色预设表 |
| **修改** | `middleware/keyboard/combo/kb_combo_map.c` | 新增组合键 |
| **修改** | `middleware/keyboard/combo/kb_fn_action.c` | 新增 Action（含电量检查） |
| **修改** | `middleware/keyboard/keyboard.c` | 背光活动通知包装函数 |
| **修改** | `drivers/power/battery.c` | 复用现有 IND_REQ 机制 |
| **修改** | `application/service/output_service.h` | 新增背光休眠事件 |
| **修改** | `application/service/output_service.c` | 背光休眠逻辑 + 低电量联动 |

**文件数量对比**：

| 版本 | 新增文件 | 修改文件 | 总计 |
|------|----------|----------|------|
| V1.1 | 8 个 | 6 个 | 14 个 |
| **V1.2** | **3 个** | **9 个** | **12 个** |

### 3.3 模块依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                     keyboard.c (主循环)                      │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │ matrix   │    │ combo    │    │ output   │
    │ _task()  │    │ _engine()│    │ _service │
    └──────────┘    └────┬─────┘    └────┬─────┘
                         │               │
                         ▼               ▼
                   ┌──────────┐    ┌──────────┐
                   │ fn_action│    │ backlight│
                   │          │    │ (driver) │
                   └────┬─────┘    └────┬─────┘
                        │               │
                        ▼               ▼
                   ┌──────────┐    ┌──────────┐
                   │ indicator│    │ 背光休眠 │
                   └──────────┘    │ (5s超时) │
                                   └──────────┘

OSAL 事件流（复用现有机制）：
battery.c ──(IND_REQ_LOW_BATTERY)──▶ output_service.c
                                           │
                               ┌───────────┴───────────┐
                               ▼                       ▼
                      背光关闭 + 红灯闪          indicator_set()
```

**层级边界优化**：
- Middleware 层通过 OSAL 事件与 Application 层通信，避免直接依赖
- `keyboard.c` 提供包装函数 `keyboard_note_backlight_activity()` 供 `fn_action.c` 调用

---

## 4. 组件设计

### 4.1 背光休眠功能（整合到 output_service）

#### 4.1.1 设计思路

现有 `output_service.c:76-84` 已有背光事件占位：

```c
if (events & OUTPUT_BACKLIGHT_BRIGHTNESS_EVT) { /* Placeholder */ }
if (events & OUTPUT_BACKLIGHT_COLOR_EVT) { /* Placeholder */ }
```

**优化方案**：复用这些事件槽位，在 `output_service` 中实现背光休眠逻辑。

#### 4.1.2 output_service.h 事件定义

**任务**: 添加背光休眠 OSAL 事件

```c
/* ========== 背光休眠配置 ========== */
#ifndef BACKLIGHT_IDLE_TIMEOUT_MS
#define BACKLIGHT_IDLE_TIMEOUT_MS   5000   // 5秒无操作熄灭
#endif

/* ========== OSAL 事件定义（新增） ========== */
// 在现有 output_task_event_t 枚举中添加：
// OUTPUT_BACKLIGHT_SLEEP_EVT  = 1 << 10,  // 背光休眠定时事件

/**
 * @brief 通知背光有活动（按键/触控时调用）
 * @note 内部会重启 5 秒 OSAL 定时器
 */
void output_service_note_backlight_activity(void);

/**
 * @brief 设置低电量模式（控制背光）
 * @param low true=低电量，强制关背光
 */
void output_service_set_backlight_low_battery(bool low);
```

#### 4.1.3 output_service.c 实现 (`application/service/output_service.c`)

**设计原则**: 使用 OSAL 定时事件，禁止轮询。

```c
/* ========== 外部变量 ========== */
extern uint8_t output_taskID;

/* ========== 背光休眠配置 ========== */
#ifndef BACKLIGHT_IDLE_TIMEOUT_MS
#define BACKLIGHT_IDLE_TIMEOUT_MS   5000   // 5秒无操作熄灭
#endif

/* ========== 背光休眠状态 ========== */
static bool bl_low_battery_mode = false;
static bool bl_sleep_active = false;

/**
 * @brief 通知背光有活动（按键/触控时调用）
 *
 * 实现方式（OSAL 事件调度）：
 * 1. 取消当前的休眠定时器
 * 2. 重新启动 5 秒休眠定时器
 * 3. 如果背光关闭且不是低电量模式，重新点亮
 */
void output_service_note_backlight_activity(void) {
    // 取消当前定时器（如果存在）
    OSAL_ClearEvent(output_taskID, OUTPUT_BACKLIGHT_SLEEP_EVT);

    // 重新启动 5 秒休眠定时器
    OSAL_StartTimer(output_taskID, OUTPUT_BACKLIGHT_SLEEP_EVT,
                    BACKLIGHT_IDLE_TIMEOUT_MS);

    // 如果背光关闭且不是低电量模式，重新点亮
    if (bl_sleep_active && !bl_low_battery_mode) {
        backlight_enable();
        bl_sleep_active = false;
    }
}

/**
 * @brief 设置低电量模式（控制背光）
 * @param low true=低电量，强制关背光
 */
void output_service_set_backlight_low_battery(bool low) {
    bl_low_battery_mode = low;

    if (low) {
        // 低电量：取消定时器，关闭背光
        OSAL_ClearEvent(output_taskID, OUTPUT_BACKLIGHT_SLEEP_EVT);
        backlight_disable();
        bl_sleep_active = true;
    } else {
        // 电量恢复：如果有背光活动，重新启动定时器
        if (backlight_is_enabled()) {
            OSAL_StartTimer(output_taskID, OUTPUT_BACKLIGHT_SLEEP_EVT,
                            BACKLIGHT_IDLE_TIMEOUT_MS);
        }
        bl_sleep_active = false;
    }
}

/**
 * @brief 初始化背光休眠服务
 * @note 在 output_service_init() 中调用
 */
static void backlight_sleep_init(void) {
    bl_low_battery_mode = false;
    bl_sleep_active = false;

    // 启动初始 5 秒休眠定时器
    OSAL_StartTimer(output_taskID, OUTPUT_BACKLIGHT_SLEEP_EVT,
                    BACKLIGHT_IDLE_TIMEOUT_MS);
}

// 在 output_process_event 中处理休眠事件
uint16_t output_process_event(uint8_t task_id, uint16_t events) {
    // ... 现有事件处理 ...

    // ========== 背光休眠定时事件（OSAL 事件驱动） ==========
    if (events & OUTPUT_BACKLIGHT_SLEEP_EVT) {
        // 5 秒超时，关闭背光
        if (!bl_low_battery_mode && backlight_is_enabled()) {
            backlight_disable();
            bl_sleep_active = true;
        }
        return (events ^ OUTPUT_BACKLIGHT_SLEEP_EVT);
    }

    // ... 其他事件 ...
}
```

#### 4.1.3 keyboard.c 包装函数 (`middleware/keyboard/keyboard.c`)

```c
// 声明外部函数（避免包含 output_service.h）
extern void output_service_note_backlight_activity(void);

/**
 * @brief 通知背光服务有活动发生
 *
 * 此函数供 Middleware 层（如 kb_fn_action.c）调用，
 * 避免直接依赖 Application 层。
 */
void keyboard_note_backlight_activity(void) {
    output_service_note_backlight_activity();
}
```

**层级依赖图**:
```
Application (output_service.c)
    │
    └──▶ backlight.c (Driver)

Middleware (kb_fn_action.c)
    │
    └──▶ keyboard.c (wrapper)
              │
              └──▶ output_service (通过包装函数，保持边界清晰)
```

### 4.2 颜色扩展

#### 4.2.1 枚举定义 (`drivers/output/backlight/backlight.h`)

```c
/**
 * @brief 预设颜色枚举（13色）
 *
 * 顺序：红-暗红-粉红-紫-靛青-深蓝-蓝-青蓝-青绿-绿-淡黄-橙-白
 */
typedef enum {
    BL_COLOR_RED = 0,       // 红
    BL_COLOR_DARK_RED,      // 暗红
    BL_COLOR_PINK,          // 粉红
    BL_COLOR_PURPLE,        // 紫
    BL_COLOR_INDIGO,        // 靛青
    BL_COLOR_DARK_BLUE,     // 深蓝
    BL_COLOR_BLUE,          // 蓝
    BL_COLOR_CYAN_BLUE,     // 青蓝
    BL_COLOR_CYAN_GREEN,    // 青绿
    BL_COLOR_GREEN,         // 绿
    BL_COLOR_LIGHT_YELLOW,  // 淡黄
    BL_COLOR_ORANGE,        // 橙
    BL_COLOR_WHITE,         // 白
    BL_COLOR_COUNT          // = 13
} bl_preset_color_t;

/* 兼容性别名 */
#define BL_COLOR_YELLOW         BL_COLOR_LIGHT_YELLOW
#define BL_COLOR_CYAN           BL_COLOR_CYAN_GREEN
#define BL_COLOR_MAGENTA        BL_COLOR_PURPLE
```

#### 4.2.2 颜色表 (`drivers/output/backlight/backlight.c`)

```c
static const bl_rgb_t preset_colors[BL_COLOR_COUNT] = {
    [BL_COLOR_RED]        = {100,   0,   0},  // 红
    [BL_COLOR_DARK_RED]   = { 50,   0,   0},  // 暗红
    [BL_COLOR_PINK]       = {100,  50,  80},  // 粉红
    [BL_COLOR_PURPLE]     = { 60,   0, 100},  // 紫
    [BL_COLOR_INDIGO]     = { 30,   0, 100},  // 靛青
    [BL_COLOR_DARK_BLUE]  = {  0,   0,  80},  // 深蓝
    [BL_COLOR_BLUE]       = {  0,   0, 100},  // 蓝
    [BL_COLOR_CYAN_BLUE]  = {  0,  50, 100},  // 青蓝
    [BL_COLOR_CYAN_GREEN] = {  0, 100, 100},  // 青绿
    [BL_COLOR_GREEN]      = {  0, 100,   0},  // 绿
    [BL_COLOR_LIGHT_YELLOW]= { 80,  80,   0},  // 淡黄
    [BL_COLOR_ORANGE]     = {100,  50,   0},  // 橙
    [BL_COLOR_WHITE]      = {100, 100, 100},  // 白
};
```

### 4.3 Fn 组合键扩展（整合到 kb_fn_action.c）

#### 4.3.1 新增 Action 函数 (`middleware/keyboard/combo/kb_fn_action.c`)

```c
#include "backlight.h"
#include "keyboard.h"              // 获取包装函数
#include "battery.h"
#include "indicator.h"
#include "indicator_config.h"

/**
 * @brief Fn+右Shift: 切换背光亮度（4档循环）
 */
uint8_t Backlight_Level_Up(uint16_t* add_keys) {
    backlight_level_step();                   // OFF→LOW→MEDIUM→HIGH→OFF
    keyboard_note_backlight_activity();       // 重置休眠计时器
    return 0;
}

/**
 * @brief Fn+右Enter: 切换背光颜色（13色循环）
 */
uint8_t Backlight_Color_Next(uint16_t* add_keys) {
    if (backlight_is_enabled()) {
        backlight_color_step();               // 只有背光亮时才切换
    }
    keyboard_note_backlight_activity();       // 重置休眠计时器
    return 0;
}

/**
 * @brief Fn+右Cmd: 电量检查（闪烁1-4次）
 */
uint8_t Battery_Check(uint16_t* add_keys) {
    uint8_t percentage = battery_get_percentage();
    uint8_t blinks;

    // 计算闪烁次数
    if (percentage >= 75)      blinks = 4;
    else if (percentage >= 50) blinks = 3;
    else if (percentage >= 25) blinks = 2;
    else                       blinks = 1;

    // 触发指示灯闪烁
    ind_effect_t effect = IND_BLINK_CUSTOM(200, 200, blinks);
    indicator_set(LED_BAT, &effect);

    return 0;
}

/**
 * @brief 自定义键: 唤起 Siri (Consumer 0xCF)
 */
uint8_t Siri_Invoke(uint16_t* add_keys) {
    add_keys[0] = M_SIRI;  // HCI_CONSUMER_CODE = 0xCF
    return 1;
}
```

#### 4.3.2 Combo 映射表更新 (`middleware/keyboard/combo/kb_combo_map.c`)

> **长按时间说明**: `COMBO_LONG_TICKS(..., 300, ...)` = 300 ticks = 3 秒（1 tick = 10ms）

```c
// 新增组合键枚举
enum combos {
    // ... 现有项 ...

    // KB904 新增
    FN_RIGHT_SHIFT_ID,    // Fn+右Shift: 亮度切换
    FN_RIGHT_ENTER_ID,    // Fn+右Enter: 颜色切换
    FN_RIGHT_CMD_ID,      // Fn+右Cmd: 电量检查
    CUSTOM_KEY_ID,        // 自定义键: Siri

    COMBOS_END,
};

// 组合键定义
const uint16_t PROGMEM FN_RIGHT_SHIFT_combo[] = { S_FN_KEY, KC_RIGHT_SHIFT, COMBO_END };
const uint16_t PROGMEM FN_RIGHT_ENTER_combo[] = { S_FN_KEY, KC_RIGHT_ENTER, COMBO_END };
const uint16_t PROGMEM FN_RIGHT_CMD_combo[]   = { S_FN_KEY, KC_RIGHT_GUI, COMBO_END };
const uint16_t PROGMEM CUSTOM_KEY_combo[]     = { KC_APPLICATION, COMBO_END };

// 组合键数组
combo_t key_combos[] = {
    // ... 现有项 ...

    [FN_RIGHT_SHIFT_ID] = COMBO(FN_RIGHT_SHIFT_combo, PRESS_DOWN, Backlight_Level_Up),
    [FN_RIGHT_ENTER_ID] = COMBO(FN_RIGHT_ENTER_combo, PRESS_DOWN, Backlight_Color_Next),
    [FN_RIGHT_CMD_ID]   = COMBO(FN_RIGHT_CMD_combo,   PRESS_DOWN, Battery_Check),
    [CUSTOM_KEY_ID]     = COMBO(CUSTOM_KEY_combo,     PRESS_DOWN, Siri_Invoke),
};
```

### 4.4 指示灯配置（条件编译）

#### 4.4.1 修改 `keyboards/indicator_config.h`

```c
#pragma once

#include "gpio.h"
#include <stdbool.h>

/* ============ 产品区分 ============ */

#if PRODUCT_ID == 0x0904  // KB904

/* KB904: 4 颗 LED */
#define IND_LED_COUNT   4

/* LED 索引定义 */
#define LED_CAPS        0   // 白灯：大写锁定
#define LED_BT          1   // 蓝灯：蓝牙状态
#define LED_POWER_RED   2   // 红灯：电源/低电量
#define LED_POWER_GRN   3   // 绿灯：充满电（只读）

/* 兼容性别名 */
#define LED_BAT         LED_POWER_RED
#define LED_CHARGE      LED_POWER_RED

/* LED 硬件配置表 */
static const ind_led_def_t ind_led_table[IND_LED_COUNT] = {
    {A15, true},   // 白灯：大写锁定
    {B22, true},   // 蓝灯：蓝牙状态
    {B23, true},   // 红灯：电源/低电量
    {A14, true},   // 绿灯：充满（仅读取）
};

#else  // N0046 及其他产品

/* N0046: 2 颗 LED */
#define IND_LED_COUNT   2

#define LED_WHITE       0
#define LED_RED         1
#define LED_CAPS        LED_WHITE
#define LED_BAT         LED_RED
#define LED_CHARGE      LED_RED

static const ind_led_def_t ind_led_table[IND_LED_COUNT] = {
    {A15, true},   // 白灯
    {B23, true},   // 红灯
};

#endif
```

### 4.5 低电量联动（复用现有机制）

#### 4.5.1 battery.c 发送请求 (`drivers/power/battery.c`)

**复用现有 `output_service_request_indicator()` 机制**：

```c
#include "output_service.h"

void battery_task(void) {
    // ... 现有电压检测逻辑 ...

    uint8_t current_state = battery_get_power_state();
    static uint8_t last_state = BAT_POWER_NORMAL;

    if (current_state != last_state) {
        last_state = current_state;

        if (current_state == BAT_POWER_LOW) {
            // 复用现有机制：发送低电量指示请求
            output_service_request_indicator(IND_REQ_LOW_BATTERY, 0);
        } else if (current_state == BAT_POWER_NORMAL) {
            // 电量恢复
            output_service_request_indicator(IND_REQ_BATTERY_NORMAL, 0);
        }
    }
}
```

#### 4.5.2 扩展 output_service 请求映射 (`application/service/output_service.c`)

```c
// 扩展现有 ind_req_map
static const ind_req_map_t ind_req_map[] = {
    // ... 现有映射 ...
    { IND_REQ_BT_PAIRING,        LED_WHITE, &IND_BLINK_SLOW },
    { IND_REQ_BT_RECONNECTING,   LED_WHITE, &IND_BLINK_FAST },
    { IND_REQ_BT_CONNECTED,      LED_WHITE, &IND_ON         },
    { IND_REQ_BT_DISCONNECTED,   LED_WHITE, &IND_OFF        },
    { IND_REQ_CAPS_ON,           LED_WHITE, &IND_ON         },
    { IND_REQ_CAPS_OFF,          LED_WHITE, &IND_OFF        },
    { IND_REQ_CAPS_DISCONNECTED, LED_WHITE, &IND_BLINK_SLOW },
    { IND_REQ_LOW_BATTERY,       LED_BAT,   &IND_BLINK_SLOW },  // 复用
    { IND_REQ_BATTERY_NORMAL,    LED_BAT,   &IND_OFF        },  // 复用
};

// 在 output_service_request_indicator 中添加背光联动
void output_service_request_indicator(ind_req_type_t type, uint8_t param) {
    (void)param;

    for (uint8_t i = 0; i < (sizeof(ind_req_map) / sizeof(ind_req_map[0])); i++) {
        if (ind_req_map[i].type == type) {
            uint8_t led_id = ind_req_map[i].led_id;

            ind_pending[led_id].effect = ind_req_map[i].effect;
            ind_pending[led_id].dirty = true;
            OSAL_SetEvent(output_taskID, OUTPUT_INDICATOR_EVT);

            // 新增：低电量时联动关闭背光
            if (type == IND_REQ_LOW_BATTERY) {
                output_service_set_backlight_low_battery(true);
            } else if (type == IND_REQ_BATTERY_NORMAL) {
                output_service_set_backlight_low_battery(false);
            }

            return;
        }
    }
}
```

---

## 5. 数据流与错误处理

### 5.1 按键事件流

```
用户按键
    │
    ▼
matrix_scan() ──▶ debounce() ──▶ action_exec()
                                        │
                                        ▼
                               combo_engine()
                                        │
                    ┌───────────────────┼───────────────────┐
                    ▼                   ▼                   ▼
               单键 HID           Fn 组合键            长按 3s
                    │                   │                   │
                    ▼                   ▼                   ▼
             report_send()      fn_action()         配对/恢复出厂
```

### 5.2 背光控制流（OSAL 事件调度）

```
┌──────────────────────────────────────────────────────────────┐
│                   活动源 (Activity Sources)                   │
├──────────────┬──────────────┬──────────────┬─────────────────┤
│  按键事件    │  触控事件    │  唤醒事件    │  背光按键       │
└──────┬───────┴──────┬───────┴──────┬───────┴──────┬──────────┘
       └──────────────┴──────────────┴──────────────┘
                              │
                              ▼
           keyboard_note_backlight_activity()
                              │
                              ▼
           output_service_note_backlight_activity()
                              │
                   ┌──────────┴──────────┐
                   ▼                     ▼
         OSAL_ClearEvent()        如休眠中则唤醒背光
                   │
                   ▼
         OSAL_StartTimer(5000ms)  ← 重置 5 秒定时器
                   │
                   │  ┌─────────────────────────────────┐
                   │  │        5 秒内无活动              │
                   │  └─────────────────────────────────┘
                   │                     │
                   ▼                     ▼
         OSAL 触发 OUTPUT_BACKLIGHT_SLEEP_EVT
                   │
                   ▼
         backlight_disable() + bl_sleep_active = true

┌──────────────────────────────────────────────────────────────┐
│                      低电量强制关闭                           │
├──────────────────────────────────────────────────────────────┤
│  output_service_set_backlight_low_battery(true)              │
│       │                                                      │
│       ├────── OSAL_ClearEvent() ──────┐                       │
│       └────── backlight_disable() ───┘                       │
└──────────────────────────────────────────────────────────────┘
```

### 5.3 错误处理

| 错误类型 | 检测方式 | 处理策略 |
|----------|----------|----------|
| EEPROM 写入失败 | CRC 校验失败 | 重试 3 次，失败后使用默认值 |
| 电量 ADC 异常 | 值超出范围 | 返回 50% 默认值 |
| 蓝牙配对超时 | 60s 计时器 | 停止广播，进入休眠 |
| 蓝牙回连超时 | 60s 计时器 | 进入休眠，唤醒后重试 |

### 5.4 恢复出厂设置

```c
void factory_reset_execute(void) {
    // 1. 指示灯反馈
    indicator_set(LED_CAPS, &IND_BLINK_3);
    indicator_set(LED_BT, &IND_BLINK_3);

    // 2. 清除蓝牙配对记录
    bt_driver_clear_bonding();

    // 3. 重置存储为默认值
    storage_factory_reset();

    // 4. 重置背光（白光 2 档）
    backlight_set_preset_level(BL_LEVEL_MEDIUM);
    backlight_set_preset_color(BL_COLOR_WHITE);

    // 5. 延迟后重启
    wait_ms(500);
    system_restart();
}
```

---

## 6. 实施计划

### 6.1 阶段一：基础设施准备 (1h)

| 步骤 | 任务 | 涉及文件 | 验证标准 |
|------|------|----------|----------|
| 1.1 | 创建 KB904 配置目录 | `keyboards/kb904/config.h` | 编译通过 |
| 1.2 | 配置 GPIO/矩阵 | `keyboards/kb904/config_hw.h` | GPIO 初始化无报错 |
| 1.3 | 指示灯条件编译 | `keyboards/indicator_config.h` | 4 灯配置生效 |

### 6.2 阶段二：背光模块扩展 (2h)

| 步骤 | 任务 | 涉及文件 | 验证标准 |
|------|------|----------|----------|
| 2.1 | 扩展颜色枚举 | `backlight.h` | 编译通过 |
| 2.2 | 扩展颜色表 | `backlight.c` | 13色循环正确 |
| 2.3 | 背光休眠逻辑 | `output_service.c` | 5秒后熄灭 |
| 2.4 | 唤醒逻辑 | `output_service.c` | 按键立即亮 |

### 6.3 阶段三：Fn 组合键 (2.5h)

| 步骤 | 任务 | 涉及文件 | 验证标准 |
|------|------|----------|----------|
| 3.1 | 包装函数 | `keyboard.c` | 编译通过 |
| 3.2 | Backlight_Level_Up | `kb_fn_action.c` | Fn+右Shift 亮度切换 |
| 3.3 | Backlight_Color_Next | `kb_fn_action.c` | Fn+右Enter 颜色切换 |
| 3.4 | Battery_Check | `kb_fn_action.c` | Fn+右Cmd 闪烁1-4次 |
| 3.5 | Siri_Invoke | `kb_fn_action.c` | 自定义键唤起 Siri |
| 3.6 | 更新 Combo 映射 | `kb_combo_map.c` | 所有组合键生效 |

### 6.4 阶段四：指示灯与低电量联动 (1.5h)

| 步骤 | 任务 | 涉及文件 | 验证标准 |
|------|------|----------|----------|
| 4.1 | 蓝牙状态回调 | `wireless.c` | 配对快闪/回连慢闪 |
| 4.2 | 低电量联动 | `battery.c` + `output_service.c` | ≤20% 关背光+红灯闪 |
| 4.3 | 大写锁定指示 | `keyboard.c` | Caps 灯同步 |

### 6.5 阶段五：恢复出厂 (1.5h)

| 步骤 | 任务 | 涉及文件 | 验证标准 |
|------|------|----------|----------|
| 5.1 | Factory_Reset Action | `kb_sys_action.c` | ESC+= 长按3s |
| 5.2 | 清除配对记录 | `bt_driver.c` | 配对记录清空 |
| 5.3 | 重置背光默认值 | `output_service.c` | 白光2档 |

### 6.6 阶段六：集成测试 (6h)

| 步骤 | 任务 | 验证标准 |
|------|------|----------|
| 6.1 | 背光功能测试 | 13色、4档亮度、5秒休眠、唤醒 |
| 6.2 | 组合键测试 | 所有 Fn 组合键正常 |
| 6.3 | 指示灯测试 | 蓝牙/大写/低电状态正确 |
| 6.4 | 蓝牙连接测试 | 配对/回连/休眠/唤醒 |
| 6.5 | iPad 实机测试 | 所有功能正常 |

### 6.7 工作量汇总

| 阶段 | 预估工时 |
|------|----------|
| 阶段一：基础设施 | 1h |
| 阶段二：背光扩展 | 2h |
| 阶段三：Fn 组合键 | 2.5h |
| 阶段四：指示灯与低电量 | 1.5h |
| 阶段五：恢复出厂 | 1.5h |
| 阶段六：集成测试 | 6h |
| **总计** | **14.5h ≈ 2 人日** |

**对比 V1.1**：从 20.5h 减少到 14.5h，节省约 30% 工时。

---

## 附录 A: KB904 GPIO 映射

| 功能 | GPIO | 说明 |
|------|------|------|
| 矩阵行 R0-R7 | PB9-PB2 | 输出 |
| 矩阵列 C0-C15 | PB17-10, PA7-0, PA15, PB1 | 输入上拉 |
| 触控板 I2C SDA | PB20 | 双向 |
| 触控板 I2C SCL | PB21 | 输出 |
| 触控板 INT | PA3 | 输入中断 |
| 背光 R | PB0 | PWM |
| 背光 G | PA12 | PWM |
| 背光 B | PA13 | PWM |
| 大写指示灯 (白) | A15 | GPIO |
| 蓝牙指示灯 (蓝) | B22 | GPIO |
| 电源指示灯 (红) | B23 | GPIO |
| 充满指示灯 (绿) | A14 | GPIO (只读) |
| 电池 ADC | PA14 | ADC |
| 电源保持 | PB19 | GPIO |

---

## 附录 B: 修订历史

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-03-17 | 初始版本 |
| V1.1 | 2026-03-17 | 评审修订：添加键码定义、修复层级违规、补充Combo宏说明 |
| V1.2 | 2026-03-18 | 架构优化：整合 backlight_service 到 output_service，复用现有低电量机制，减少新增文件（8→3），节省约 30% 工时 |

---

*文档结束*
